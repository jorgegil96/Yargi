data class Pair(int first, int second)

data class Quadruple(int first, int second, int third, int fourth)

class Test {

    Pair pair;

    main() {
        Quadruple quad;

        quad = Quadruple(1, 2, 3, 4);
        pair = Pair(8, 10);

        if (pair.second < quad.fourth) {
            write(pair.first + quad.first);
        } else {
            write(quad.second + quad.third + pair.second);
        }
    }

}